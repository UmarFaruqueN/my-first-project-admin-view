import React from "react";
import { TopHeader, LoginForm } from "../Components";

const Login = () => {
     return (
          <>
               <TopHeader />
               <LoginForm />
          </>
     );
};

export default Login;
