import React from "react";
import { Layout, OrderManagement } from "../Components";

const OrderHome = () => {
     return (
          <Layout>
               <OrderManagement />
          </Layout>
     );
};

export default OrderHome;
