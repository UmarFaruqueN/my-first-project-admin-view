import React from "react";
import { UserManagement, Layout } from "../Components";

const UserHome = () => {
     return (
          <Layout>
               <UserManagement />
          </Layout>
     );
};

export default UserHome;
